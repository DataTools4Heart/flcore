num_clients: 3
num_rounds: 3
model: linear_models #linear_models, xgb, random_forest, logistic_regression (old version)
dataset: cvd # mnist, cvd, libsvm
data_path: dataset/

experiment:
  name: "test_run_1"
  results_path: "results"
  debug: True

random_forest:
  balanced_rf: True  

xgb:
  task_type: BINARY
  batch_size: 32
  num_iterations: 100
  tree_num: 500

linear_models:
  model_type: LR #elastic_net,LSVC, LR
  n_features: 9
